# Config Loader

This folder contains scripts that help prepare config so that the deployment time can be reduced.

## Features

- `load_table_metadata`: This script automates the process of loading table metadata from an Excel file to a table in a PostgeSQL database.
